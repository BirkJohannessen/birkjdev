export default class LanguageIcon {
    public name: string;
    public filename: string;

    constructor(
        name: string,
        filename: string,
    ) {
        this.name = name;
        this.filename = filename;
    }
}
