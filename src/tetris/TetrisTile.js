export default class CommandProcessor {
    constructor(tileState, _parentBlock, _color, _asciiValue) {
        this.tileState = tileState;
        this.tileReflect = 0;
        this.debug = 0;
    }
}

